<h3>{{name}} <small>{{vendor}}</small></h3>
<p><span class="auto-field">{{vendor}}</span> has billed <span class="auto-field">{{sales}}</span> in sales since the beginning of fiscal year 2010 to <span class="auto-field">{{agency}}</span> for work performed by <span class="auto-field">{{name}}</span>. The table below shows each of the sales reports to the Department of Information Resources submitted by {{vendor}}.</p>

<div class="row">
  <div class="col-xs-12 col-sm-7">
    <h3>Individual sales reports</h3>
    <div class="table-responsive">
      <table class="table table-condensed">
        <thead>
          <tr>
            <th>Month</th>
            <th>Title</th>
            <th>Hours billed</th>
            <th>Rate</th>
            <th>Amount</th>
          </tr>
        </thead>
        <tbody>
          {{#each transactions}}
            <tr{{#if negative}} class="danger"{{/if}}>
              <td>{{month}}</td>
              <td>{{level}}</td>
              <td>{{hours}}</td>
              <td>{{rate}}</td>
              <td>{{amount}}</td>
            </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
  <div class="col-xs-12 col-sm-5 charts">
    <h4>Sales by month</h4>
    <div id="sales-chart"></div>
    <h4>Hours billed by month</h4>
    <div id="hours-chart"></div>
    <h4>Summary</h4>
    <table class="table">
      <tbody>
        <tr>
          <th>Total sales</th>
          <td>{{sales}}</td>
        </tr>
        <tr>
          <th>Total hours billed</th>
          <td>{{hours}}</td>
        </tr>
        <tr>
          <th>Average rate</th>
          <td>{{avgRate}}/hr.</td>
        </tr>
        <tr>
          <th>Months worked</th>
          <td>{{monthsWorked}}</td>
        </tr>
        <tr>
          <th>Average hours per month</th>
          <td>{{avgPerMonth}}</td>
        </tr>
        <tr>
          <th>Rank<br /><small>By total sales</small></th>
          <td>{{rank}} of 4,176</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
