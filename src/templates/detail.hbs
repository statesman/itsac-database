<h3>{{name}} <small>{{vendor}}</small><h3>

<h2>Billing for {{agency}}:</h2>
<table class="table">
  <thead>
    <tr>
      <th>Month</th>
      <th>Title</th>
      <th>Hours billed</th>
      <th>Rate</th>
      <th>Amount</th>
    </tr>
  </thead>
  <tbody>
    {{#each transactions}}
      <tr>
        <td>{{month}}</td>
        <td>{{level}}</td>
        <td>{{hours}}</td>
        <td>{{rate}}</td>
        <td>{{amount}}</td>
      </tr>
    {{/each}}
  </tbody>
</table>
